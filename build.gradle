subprojects {
    apply plugin: 'java'
    apply plugin: "application"

    sourceCompatibility = 17
    targetCompatibility = 17

    group 'tagged-file-manager'
    version '0.0.1'
    repositories {
        mavenCentral()
    }
}